# Weddings (admin-web) — UI

```
┌────────────────────────────────────────────────────────┐
│ Casamentos                                                │
│                                                              │
│ [Pesquisar...] [Estado ▾: Todos]                            │
│                                                              │
│ Noivos              Data          Local         Estado      │
│ Ana & (por definir)  24 Ago 2027  Lisboa        Planeamento  │
│ Miguel & Sofia       —            —             Planeamento  │
└────────────────────────────────────────────────────────┘
```

Detalhe: `Section`/`Field` reutilizados de `admin-web/partners/[id]` — dados do casamento, link para o dono em `admin-web/users/:id`, lista de colaboradores.
