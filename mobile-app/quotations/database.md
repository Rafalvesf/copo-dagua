# Quotations (mobile-app) — Modelo de Dados

Nenhuma tabela própria. Consome `quote_requests`/`proposals` via `request_quote()`/`accept_proposal()` e leitura direta (RLS) — ver `backend/quotations/database.md` e `backend/quotations/api.md` para o contrato completo.
