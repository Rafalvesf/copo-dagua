# Bookings (partner-app) — Requisitos

## Funcionalidades

- Lista de reservas, filtrável por estado.
- Detalhe: dados do casal, casamento, valores, timeline (`booking_events`).

## Fora de âmbito (MVP)

- Confirmar o sinal recebido — no MVP essa ação é só do admin (stub, `backend/bookings/api.md`), não do parceiro; o parceiro só visualiza.

## Regras de negócio

Todas herdadas de `backend/bookings/requirements.md`.
