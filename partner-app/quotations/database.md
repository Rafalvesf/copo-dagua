# Quotations (partner-app) — Modelo de Dados

Nenhuma tabela própria. Consome `quote_requests`/`proposals` via `send_proposal()` e leitura direta (RLS) — ver `backend/quotations/database.md` e `backend/quotations/api.md`.
